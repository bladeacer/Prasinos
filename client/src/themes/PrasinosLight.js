import { createTheme } from "@mui/material/styles";

const theme = createTheme({
    palette: {
        primary: {
            main: '#9f8e62'
        },
        secondary: {
            main: '#b7ccad'
        },
        accent: {
            main: '#8ab78f'
        }
    }
});

export default theme;